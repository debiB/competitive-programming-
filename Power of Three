class Solution {
    public boolean isPowerOfThree(int n) {
        if(n>0){
 double b= Math.log(n);
double bc= Math.log(3);
int val=(int)Math.round(b/bc);
if(Math.pow(3,val)==n) return true;
        }  
        return false;
    }
}
